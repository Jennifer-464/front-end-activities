// Unrelated content, got curious about memory

// localStorage.setItem("teams", JSON.stringify(rankedTeams));
// localStorage.setItem("todo", "buy a watch");

// document.cookie = 'name2=value2';
// document.cookie = 'name3=value3';


// alert(document.cookie);
// alert(localStorage["teams"]);

/*
var text = document.getElementById("text");

var element = document.createElement("h1");
var textNode = document.createTextNode(localStorage["todo"]);

element.appendChild(textNode);
text.appendChild(element);
*/

// localStorage.clear();

// https://plainenglish.io/blog/3-ways-to-store-data-in-the-browser